# Render: NODE_ENV=production would skip devDependencies.
# include=dev keeps build tooling WITHOUT dropping optional platform binaries
# (turbo needs @turbo/linux-64 on Render — do not use omit=optional).
include=dev
