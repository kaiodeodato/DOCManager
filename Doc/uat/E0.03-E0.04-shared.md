# UAT — E0.03 / E0.04 shared enums + Zod

## Objective

Verify domain contracts compile and validate at runtime.

## Preconditions

- `npm install` at repo root

## Steps

1. `npm run test -- --filter=@ac/shared`
2. From a Node REPL or test, import `DocumentStatus` and `DocumentUploadPayloadSchema` from `@ac/shared`
3. Parse a valid upload payload and an invalid one

## Expected Results

- Enum collision tests pass
- Each schema has valid/invalid/missing-field coverage
- `@ac/web` build still succeeds importing `@ac/shared`
