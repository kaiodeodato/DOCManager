# UAT — E1.01 orgs / org_members

## Objective

Confirm orgs + org_members migration and seed docs are present.

## Preconditions

- Repo cloned; Supabase CLI optional for SQL apply.

## Steps

1. Open `supabase/migrations/20260301000100_orgs_and_members.sql`
2. Confirm tables `orgs`, `org_members` and role check constraint
3. Read `Doc/architecture/orgs.md` and `supabase/seed/README.md`
4. (Optional) `npx supabase db reset`

## Expected results

- Migration defines unique `(org_id, user_id)` and roles owner/accountant/viewer
- Seed README documents two-org template

## Edge cases

- Inserting invalid role must fail check constraint when DB is up
